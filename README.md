# vimconfig

You complete need to install by ourselves.

- First the vim should support python3 (vim --version)
- Second install it with

	cd ~/.vim/bundle/YouCompleteMe
	./install.py --all (or ./install.py --clang-completer)

